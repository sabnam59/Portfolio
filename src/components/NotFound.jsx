import React from 'react'

const NotFound = (props) => {
  return (
    <div className='flex justify-center h-screen items-center font-bold text-3xl bg-black text-white'>{props.text}</div>
  )
}

export default NotFound
